'use strict';

/**
 * waitlist-user-attend service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::waitlist-user-attend.waitlist-user-attend');
