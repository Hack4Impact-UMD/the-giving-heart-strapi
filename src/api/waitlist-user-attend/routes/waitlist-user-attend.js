'use strict';

/**
 * waitlist-user-attend router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::waitlist-user-attend.waitlist-user-attend');
